<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmployeeManager">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Déclaration de VersesActivity -->
        <activity
            android:name=".ui.verses.VersesActivity"
            android:exported="true"
            android:label="Vakiteny">
        </activity>
        <activity
            android:name=".ui.fihirana.SongDetailActivity"
            android:exported="true"
            android:label="fihirana">
        </activity>
        <activity
        android:name=".ui.fihirana.SongListActivity"
        android:exported="true"
        android:label="fihirana">
        </activity>
        <activity android:name=".ui.mofonaina.MofonainaActivity" android:label="Mofonaina" />
    </application>

</manifest>